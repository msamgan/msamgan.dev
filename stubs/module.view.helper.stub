export const columns = ['Name', 'Actions']

export const dataObject = ({camel}) => {
    return {
        name: {camel} ? {camel}.name : '',
    }
}

export const pageObject = ({camel}) => {
    return {
        title: {camel} ? 'Edit {title}' : 'Create {title}',
    }
}
